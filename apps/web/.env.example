# =========================================================
# Configuracion del frontend (Vite) — apps/web
# Copia este archivo a ".env". El .env real NO se commitea.
# Solo las variables con prefijo VITE_ llegan al navegador.
# =========================================================

# URL base de la API (NestJS). En dev apunta al backend local.
VITE_API_URL=http://localhost:3000/api
